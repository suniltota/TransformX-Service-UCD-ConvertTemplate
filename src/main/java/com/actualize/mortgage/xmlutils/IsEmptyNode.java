package com.actualize.mortgage.xmlutils;

import org.w3c.dom.Node;

/**
 * Interface <code>IsEmptyNode </code> execute as interface which declare empty node
 * 
 */
public interface IsEmptyNode {
	public boolean isEmptyNode(Node node);
}
